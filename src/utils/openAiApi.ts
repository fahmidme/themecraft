// src/utils/openAiApi.ts
import axios from 'axios';

export const fetchThemeStyles = async (description: string) => {
  // Placeholder for OpenAI API call
  // This will be replaced with the actual API call logic
  return new Promise(resolve => {
    setTimeout(() => {
      resolve(description); // This will be replaced with the actual API response
    }, 1000);
  });
};
